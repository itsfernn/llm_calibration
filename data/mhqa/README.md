# Dataset Description

This folder contains the data files related to the main experiment runs.

**CSV files:**
    - Each file corresponds with a run
    - Each line corresponds to a question in the dataset
    - Contains the question, answer, response, the parsed prediction, and metrics like exact match and f1 score

**data.json:** This file contains metadata about the run, including:
    - dataset
    - model
    - prompt

